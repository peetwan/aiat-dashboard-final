{
  "contract_version": "1.0",
  "source_id": "f3_housing_portal",
  "connector": "app.connectors.housing_ckan:HousingCkanConnector",
  "transport": "ckan_csv",
  "dataset_grains": [
    {
      "key_pattern": "{package_id}:{resource_id}",
      "grain_th": "หนึ่งแถวใน CKAN CSV resource ที่ได้รับอนุญาตให้ใช้ค่า",
      "identity_fields": ["resource_id", "row_number"],
      "geography_fields": ["province", "district", "subdistrict", "adm1_pcode", "adm2_pcode", "adm3_pcode"],
      "as_of_fields": ["year", "fiscal_year", "last_modified"]
    }
  ],
  "completeness_checks": [
    "ต้องพบ 7 packages และ 41 unique resources",
    "ต้อง parse 39 value-approved resources ได้ 7,259 แถวก่อน commit",
    "ห้าม commit บางส่วนเมื่อ MAX_RECORDS_PER_SOURCE ต่ำกว่าจำนวนจริง"
  ],
  "privacy": {
    "candidate_only": true,
    "contact_value_scan": true,
    "forbidden_public_fields": ["email", "phone", "address", "token", "password", "cookie", "authorization", "birth_year", "income", "respondent_id", "citizen", "national_id"]
  },
  "notes_th": "Demand person-level values และ policy-assessment values ไม่อยู่ใน value-approved connector lane"
}
