{
  "contract_version": "1.0",
  "source_id": "f2_apptech_mru",
  "connector": "app.connectors.apptech_mru:ApptechMruConnector",
  "transport": "http_form_json",
  "dataset_grains": [
    {
      "key_pattern": "innovation",
      "grain_th": "นวัตกรรมหนึ่งรายการ",
      "identity_fields": ["innovationid"],
      "geography_fields": ["province", "provinceid"],
      "as_of_fields": ["updatedate", "year"]
    },
    {
      "key_pattern": "requirement",
      "grain_th": "ความต้องการหนึ่งรายการ",
      "identity_fields": ["requirementid"],
      "geography_fields": ["province", "provinceid"],
      "as_of_fields": ["updatedate", "year"]
    },
    {
      "key_pattern": "news",
      "grain_th": "ข่าวสาธารณะหนึ่งรายการ",
      "identity_fields": ["newsid"],
      "geography_fields": [],
      "as_of_fields": ["newsdate", "updatedate"]
    }
  ],
  "completeness_checks": [
    "จำนวน unique ID ต้องเท่ากับ totaldata ทุก dataset",
    "totaldata ต้องไม่เปลี่ยนระหว่าง pagination",
    "response ทุกแถวต้องเป็น object และมี ID"
  ],
  "privacy": {
    "candidate_only": true,
    "contact_value_scan": true,
    "forbidden_public_fields": ["email", "phone", "address", "token", "password", "cookie", "authorization", "citizen", "national_id"]
  }
}
